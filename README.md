# becomes

TypeScript package scaffolded for Bun, oxlint, and oxfmt.

## Scripts

- `bun run build` compiles TypeScript into `dist`.
- `bun run lint` runs oxlint.
- `bun run format` formats the package with oxfmt.
- `bun run check` verifies formatting, linting, and TypeScript compilation.

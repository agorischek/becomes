# Changelog

## 0.1.0

- Added explicit and implicit schema history builders.
- Added typed document definitions with `open`, `save`, `create`, `migrate`,
  `validate`, and `inspect`.
- Added typed errors, schema adapter support, inference helpers, runtime tests,
  type-level tests, and enforced 100% coverage gates.
